/**
 * نقطة الدخول للمضيفين الذين ما زالوا يشغّلون `node index.js`
 * (مثل إعدادات Vercel القديمة). النسخة الكاملة للـ API القديم محفوظة في:
 *   index.legacy.js
 *
 * السيرفر الموحّد: JWT، place-types، places، ratings، الطلبات، لوحة الإدارة، إلخ.
 */
import './src/server.js';
