# انسخ هذا الملف إلى .env وعدّل القيم
# في Windows: copy .env.example .env

# اتصال PostgreSQL
# إذا استخدمت pgAdmin، عادةً يكون المستخدم postgres وكلمة المرور التي حددتها عند التثبيت
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/tulkarm-map

# منفذ الخادم
PORT=3000
